[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "somelib"
version = "0.0.1"

[tool.setuptools]
ext-modules = [
    {name = "_somelib", sources = ["src/diplomat_runtime.c"]}
]